# 待改进 & 灵感
> Optimization & Ideas

这里记录一些 【已经实现但可以优化】 / 【迸发灵感】/ 【存在部分异常】 的项目。
已完成的，请标记完成。

## 更多的拖拽与点击

- [x] Task 界面下的 Kanban View 实现了拖拽功能，可以切换状态。新目标：在同一个状态内，还可以通过上下拖拽自由排序。
- [x] Kanban View 将任务做成了卡片，点击卡片是可以编辑该任务的。发散这个优点，对于 List View 下的每一项任务，直接点击本条目也可以编辑（即：除了点击 Edit 按钮外，直接点击任务本身也可以编辑）
- [x] 为 List View 下的任务条目，也增加拖拽，实现上下自由排序。
- [x] 在 Dash Board 的 My Tasks 下，同样实现【点击可编辑】和【上下拖拽以排序】的效果。
- [x] 注意：关于【拖拽排序】的设想与新增的【按字段排序】的冲突：
  - 在 Task 界面下，由于现在增加了自定义筛选（搜索标题或标签，可清除）、排序（不可清除）等功能，和上述设计的“理念”会有不合理的冲突
  - 至少为按字段排序增加一个“默认排序”（不按照任何字段排序）
  - 或者更进一步，增加一个重置筛选按钮，清除所有筛选与字段排序。

## 沉浸式模式优化

- [] 导入更多的白噪音种类，并优化播放模式：自由选择一种声音、用户导入音声、随机播放所有

## 番茄钟与任务绑定

- [x] 已实现对具体任务开启番茄钟（专注关联），并存入数据库。
- [ ] 改进建议：展示数据
- [ ] **番茄估算 (Estimation)**: 创建任务时，允许用户估算该任务需要的番茄钟数量（例如：预计 3 个番茄），并在完成后对比实际消耗。
- [x] **Zen Note (防走神便签)**: 专注时的快速记录板，捕捉稍纵即逝的想法而不中断心流。

## 专注统计深度优化

- [x] **热力图 (Heatmap)**: 类似 GitHub 的绿色方块图，直观展示用户全年的专注强度。
  - [x] 优化起始色与色阶，确保低时长（如1分钟）也清晰可见。
  - [x] 适配深色模式，使用更舒适的深绿配色。
- [ ] **标签分析**: 统计每个标签下的总专注时长，帮助用户了解时间去向（如：这一周花了 10 小时在 #Coding 上，只有 2 小时在 #Reading）。

## 快捷键系统 (Productivity)

- [ ] `N`: 快速新建任务 (New Task)
- [ ] `Space`: 暂停/开始计时器 (Toggle Timer)
- [ ] `/`: 聚焦搜索框 (Search)
- [ ] `Esc`: 关闭弹窗 (Close Dialog)

## Dashboard 日历视图

- [x] 当鼠标悬浮在一天上，应当展示当天的任务/专注状态，空值用“这天没有任务哦/还没有专注过哦”等用户友好的消息进行占位。

## 深色模式 (Dark Mode)

- [x] 全局主题切换：设计一套深色 UI，包括配色方案、组件样式调整。建议在 Phase 4 (专注于样式的版本) 中实现。

## CSS样式优化（UI 设计）

- [] 计划于 Phase 4 阶段实现

## 首页“大约时长”计算逻辑优化

- [ ] 时长计算有误，因为并不是每个番茄钟都是25分钟。现在的 Approx 时间只是简单地用次数乘以 25 分钟，并不正确。

## 子任务功能

- [x] 可以为一项任务添加子任务
- [ ] 第一次添加子任务报错，后续无影响，有待排查

## AI 助手与分析

- [] 先等待申请 API 
- [] 在网页 onload 前，发送一次请求，获取数据库数据，根据预设程序分析与给出建议……

## 移动端适配

- [] 计划于 Phase 4 阶段实现